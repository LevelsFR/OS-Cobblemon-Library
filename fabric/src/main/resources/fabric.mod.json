{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "environment": "*",
  "entrypoints": {
    "main": [
      "com.ourstory.oscobblemon.fabric.OSCobblemonLibraryFabric"
    ]
  },
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": "*",
    "fabric-language-kotlin": "*",
    "minecraft": "~${minecraft_version}",
    "java": ">=${java_version}",
    "cobblemon": ">=${cobblemon_min_version}"
  }
}
